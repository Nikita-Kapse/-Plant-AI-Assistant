"""
Abiotic Stress Classifier Module

Placeholder implementation — the actual EfficientNet model (abiotic_model.pth)
is not yet available. Replace the placeholder logic below once the model is ready.
"""


def classify_abiotic(image_path):
    """
    Classify the type of abiotic stress from a leaf image.

    Args:
        image_path (str): Path to the leaf image.

    Returns:
        tuple: (stress_type, confidence)
    """
    # TODO: Load EfficientNet model from models/abiotic_model.pth
    # and run real inference here.
    stress_type = "Abiotic Stress Detected (model not yet integrated)"
    confidence = 0.0

    return stress_type, confidence
